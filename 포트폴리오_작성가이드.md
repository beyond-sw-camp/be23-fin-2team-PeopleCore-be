# 포트폴리오 작성 가이드

> PeopleCore 포트폴리오 제작 과정에서 확립한 형식·말투·기준. 다른 프로젝트에도 동일하게 적용.

---

## 1. 전체 원칙

- **두괄식** — 결론(수치·키워드)을 먼저, 부연은 뒤
- **개조식** — 줄글 대신 짧은 불릿/번호
- **비즈니스 관점** — "뭘 만들었나"가 아니라 "어떤 문제를 해결했나, 그래서 어떤 효과가 났나"
- **말투** — `~했다` / `~이다` / `~발생` 명사형 마무리. "열심히", "노력" 금지
- **시도/비교는 면접용** — 포트폴리오엔 결론만. 면접에서 "왜 그 선택을?"에 구술로 답변

---

## 2. 성과 페이지

### 구조
```
[배지] 성과 0N · {카테고리}
{숫자}{단위}
{키워드 제목}
{부제 - 한 줄 설명}
─────────────────
{어떻게 했는지} + {결과} 한두 줄
```

### 카드 작성 기준
- **숫자**: 수치가 있으면 반드시 크게 (83%, 0건, 13종 등)
- **키워드**: 2~4글자 임팩트 있는 명사 (토큰 절감 / 외부 유출 / 이력 자동화)
- **설명 한 줄**: `{방법}으로 {결과}` 형태
  - 예) "Prompt Caching으로 전환해 호출당 입력 비용 83% 절감 · 응답 품질 동일"
  - 예) "Debezium CDC로 binlog 자동 감지해 동기화 발행 코드 0줄 · RDB 변경 즉시 ES 반영"
- **문제/해결/결과 태그 없음** — 설명 한 줄로 압축. 태그 쓰면 성과와 트러블슈팅이 겹침

### 배지 카테고리 예시
- AI 비용 / 보안 / 데이터 정합성 / 동시성 / 운영 자동화 / 설계

### 수치 없는 항목 처리
- 억지로 수치 만들지 말 것
- "State 패턴" / "OCP" 같은 설계 키워드로 대체 가능
- 단, 수치 있는 항목과 섞이면 임팩트 약해짐 → 최대 1~2개만 허용

---

## 3. 트러블슈팅 페이지

### 구조
```
[배지] Case 0N · {주제의 동시성/멀티서버/기술제약 등 구체 설명}
{제목 - 한 줄 임팩트 문장}
─────────────────
문제
{현상 한 줄}
→ {그래서 사용자/비즈니스에 미친 영향 한 줄}

원인
{기술적 근본 원인 한 줄}

해결
{뭘 했는지 - 행동 중심으로}

배운점
{한~두 문장. 개인적 인사이트. "~을 배웠다" / "~이다" 마무리}
```

### 각 섹션 작성 기준

#### 문제
- 1줄: 어떤 현상이 발생했는지 (기술적 사실)
- → 1줄: 그래서 실제로 뭐가 문제였는지 (사용자/비즈니스 영향)
- 예) "동시 기안 시 문서번호 순번 중복 발급 → 중복된 문서번호로 같은 문서를 혼동하는 문제 발생"
- 예) "서버 확장 후 다른 서버 사용자에게 SSE 알림 미전달 → 알림 누락으로 직접 확인 전까지 상황 인지 불가"

#### 원인
- 기술적 근본 원인 한 줄. 짧게.
- 예) "SSE 세션이 각 서버 메모리에 종속되어 인스턴스 간 공유 불가"
- 예) "순번을 조회 후 저장하는 사이에 다른 요청이 끼어듦"

#### 시도 (선택)
- **포트폴리오엔 생략 권장** — 텍스트 과다, 면접 질문 유도용으로 남겨둠
- 쓸 경우: `{방식 이름} → {왜 안 됐는지/어떤 문제}` 형태로 번호 목록
- 마지막 항목이 채택한 방식
- 예)
  ```
  1. 낙관적 락 → 동시 요청 많아 재시도 폭증
  2. 비관적 락 → 순번 행 잠금으로 순서 보장
  3. 비관적 락 + 3회 재시도 + UNIQUE 3중 방어 ← 채택
  ```

#### 해결
- "뭘 했다"는 **행동** 중심. 결과(수치)가 아님
- 예) "비관적 락으로 순번 행을 선점하고, 충돌 시 3회 재시도 후 UNIQUE 제약으로 중복 원천 차단"
- 예) "Kafka Consumer로 메시지를 수신해 각 서버에서 자신의 SSE 세션에 직접 전송"
- ❌ "중복 0건 달성" (결과 → 성과 페이지에 어울림)

#### 배운점
- **한~두 문장**. 짧고 인사이트 있게
- "~를 배웠다" / "~이다" / "~해야 한다" 마무리
- 기술 나열 금지. 이 경험에서 얻은 **사고방식/판단 기준**을 적을 것
- 예)
  - "트레이드오프를 알고 고르는 것이 설계다"
  - "락은 충돌 빈도와 운영 비용을 함께 따져야 한다"
  - "실시간 통신은 처음부터 분산 환경을 전제하고 설계해야 한다"
  - "인터페이스를 통일하면 구현이 달라도 같은 방식으로 쓸 수 있다"

### 배지 명명 규칙
- `Case 0N · {주제 구체 설명}` — 카테고리 단어가 아니라 주제 자체를 적기
- ❌ `Case 01 · 동시성`
- ✅ `Case 01 · 문서번호 채번의 동시성`
- ✅ `Case 02 · SSE 알림의 멀티서버 누락`
- ✅ `Case 03 · EXAONE Tool Use 미지원`

### 제목 규칙
- 임팩트 있는 한 문장. 문제 상황 또는 해결 방향을 담아서
- 예) "문서번호 채번 — 동시 기안에도 번호는 단 하나"
- 예) "SSE 알림 — 서버 늘리니 다른 서버 사용자에게 안 갔다"
- 예) "EXAONE Tool Use 미지원 — 도구 대신 구조로 해결했다"

---

## 4. 트러블슈팅 주제 선정 기준

좋은 트러블슈팅 케이스의 조건:
1. **실제로 문제가 터진 것** — 설계 결정이나 리팩토링이 아님
2. **시도가 여러 번 있었던 것** — 첫 시도가 막히고 다른 방식을 찾은 스토리
3. **비즈니스 영향이 있는 것** — 사용자가 체감하거나 운영에 영향을 준 것
4. **성과 페이지와 소재가 안 겹치는 것**

❌ 트러블슈팅으로 쓰면 안 되는 것:
- 처음부터 더 나은 방식을 선택한 것 (설계 의사결정)
- 코드 구조 개선/리팩토링
- 기능 자체를 새로 만든 것

---

## 5. 성과 vs 트러블슈팅 역할 분리

| | 성과 | 트러블슈팅 |
|--|------|------|
| 답하는 질문 | "뭘 이뤘나?" | "어떻게 사고하나?" |
| 핵심 | 결과·수치 | 과정·판단 |
| 시도/비교 | ❌ 없음 | ✅ (면접 유도용) |
| 독자 | 인사담당자 (빠르게 스캔) | 기술면접관 (딥다이브) |
| 분량 | 카드당 2~3줄 | 섹션당 1~2줄, 4개 섹션 |

---

## 6. 회고 (트러블슈팅 페이지 하단 한 줄)

### 역할
- 트러블슈팅 3개 케이스를 **관통하는 인사이트** 한 문장
- 개별 케이스의 배운점을 반복하지 말 것
- 이 프로젝트 전체에서 얻은 **사고방식/판단 기준**을 담을 것

### 작성 기준
- **한 문장** — 길면 안 읽힘
- 톤 통일 — 앞이 경험 서술이면 뒤도 `~을 배웠다` / `~을 체감했다`로 마무리
- 앞이 선언형이면 뒤도 선언형으로 (`~이다` / `~해야 한다`)
- 두 가지 패턴:
  - **경험형**: `{경험하면서}, {인사이트}임을 배웠다`
    - 예) "문제마다 여러 대안을 비교하면서, 기술 선택의 기준은 성능이 아닌 그 상황의 트레이드오프임을 배웠다"
  - **선언형**: `{인사이트} — {경험}하며 체감했다`
    - 예) "기술 선택의 기준은 성능이 아닌 그 상황의 트레이드오프다 — 문제마다 여러 대안을 비교하며 체감했다"

### 좋은 회고의 조건
- 트러블슈팅 케이스들의 **공통 맥락**에서 나올 것
- "열심히 했다" / "좋은 경험이었다" 금지
- 읽는 사람이 "이 사람 어떻게 사고하는구나"를 알 수 있을 것

---

## 7. 피해야 할 표현

| ❌ | ✅ |
|--|--|
| ~을 구현했다 | ~으로 해결했다 / ~을 설계했다 |
| 열심히 / 노력했다 | (삭제) |
| ~을 배울 수 있었다 | ~을 배웠다 |
| 최후 방어 조합 | 최후 안전망 / 3중 방어 |
| 핫스팟 | 충돌이 잦은 로직 |
| ~을 통해 달성했습니다 | ~으로 ~달성 |
